/**
 * Transcript trigger system — currently inactive.
 * Highlights are disabled until sync issues are resolved.
 * These stubs keep the interface stable so client.ts doesn't break.
 */

export function activateTourTriggers(): void {
  // no-op
}

export function setTriggerSlide(_slideId: string): void {
  // no-op
}

export function feedTranscript(_text: string): void {
  // no-op
}

export function deactivateTourTriggers(): void {
  // no-op
}

export function resetTranscriptBuffer(): void {
  // no-op
}
